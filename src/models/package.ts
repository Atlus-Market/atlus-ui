export interface Package {
  id: string;
  title: string;
  brokerUserId: string;
  createdTimestamp: string
  dataroomId: string;
  description: string
  industryId: number,
  keywords: string;
  lastModified: string;
  sellerUserId: string
  visibility: number
}
